# ──────────────────────────────────────────────
# Mingly Server Configuration
# ──────────────────────────────────────────────
# Copy this file to .env and adjust values.

# HTTP port for the Mingly API server
MINGLY_PORT=3939

# Bind address (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
MINGLY_HOST=0.0.0.0

# Directory for database and persistent storage
MINGLY_DATA_DIR=./data

# Require API key authentication (true/false)
MINGLY_REQUIRE_AUTH=false

# API key for server authentication (required if MINGLY_REQUIRE_AUTH=true)
MINGLY_API_KEY=

# Log level: debug | info | warn | error
MINGLY_LOG_LEVEL=info

# ──────────────────────────────────────────────
# RAG Server (Qdrant + Embedding)
# ──────────────────────────────────────────────

# Qdrant vector database port
QDRANT_PORT=6333

# RAG server port
RAG_SERVER_PORT=8001

# Embedding model (default: intfloat/multilingual-e5-large)
EMBEDDING_MODEL=intfloat/multilingual-e5-large

# Directory to watch for document indexing
WATCH_DIRS=./data/documents

# RAG server log level
LOG_LEVEL=INFO
